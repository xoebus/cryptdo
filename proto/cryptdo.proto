syntax = "proto3";

package cryptdo;

option go_package = "proto";

message Message {
  // key derivation
  int32 iterations = 1;
  bytes salt = 2;

  // encryption
  bytes nonce = 3;
  bytes ciphertext = 4;
}
